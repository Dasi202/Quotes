import { ShowbadgeDirective } from './showbadge.directive';

describe('ShowbadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowbadgeDirective();
    expect(directive).toBeTruthy();
  });
});
